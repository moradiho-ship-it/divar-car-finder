import {useQuery} from '@tanstack/react-query';import {ChevronDown} from 'lucide-react';import {Fragment,useState} from 'react';import {api} from '../api/client';import {faDate,faNum} from '../lib/format';import type {Paginated} from '../types';import {EmptyState,ErrorState,PageHeader,PageSkeleton,StatusBadge} from '../components/ui';
type Run={id:number;search_title:string;started_at:string;duration_seconds:number|null;listings_scanned:number;matches_found:number;new_matches:number;status:string;error_message:string};
export default function HistoryPage(){const [expanded,setExpanded]=useState<number|null>(null);const q=useQuery({queryKey:['runs'],refetchInterval:15000,queryFn:()=>api.get<Paginated<Run>>('/crawl-runs/').then(r=>r.data)});if(q.isLoading)return <PageSkeleton/>;return <div className="space-y-6"><PageHeader title="تاریخچه بررسی‌ها" description="نمای عملیاتی پایش‌ها، نتیجه هر اجرا و خطاهای قابل پیگیری." eyebrow="پایش سیستم"/>{q.isError?<ErrorState retry={()=>q.refetch()}/>:!q.data?.results.length?<EmptyState title="هنوز بررسی‌ای ثبت نشده" description="پس از اجرای اولین جستجو، وضعیت و نتیجه آن را در این بخش خواهید دید."/>:<div className="card overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-right text-sm"><thead className="muted text-xs" style={{background:'rgb(var(--background))'}}><tr>{['جستجو','زمان شروع','مدت','بررسی‌شده','منطبق','جدید','وضعیت',''].map(x=><th className="px-5 py-4 font-medium" key={x}>{x}</th>)}</tr></thead><tbody>{q.data.results.map(r=><Fragment key={r.id}><tr className="border-t hover:bg-black/[.015] dark:hover:bg-white/[.02]" style={{borderColor:'rgb(var(--border))'}}><td className="px-5 py-4 font-semibold">{r.search_title}</td><td className="muted px-5 py-4">{faDate(r.started_at)}</td><td className="px-5 py-4">{r.duration_seconds==null?'—':`${faNum(Math.round(r.duration_seconds))} ثانیه`}</td><td className="px-5 py-4">{faNum(r.listings_scanned)}</td><td className="px-5 py-4">{faNum(r.matches_found)}</td><td className="px-5 py-4 font-semibold text-emerald-700 dark:text-emerald-300">{faNum(r.new_matches)}</td><td className="px-5 py-4"><StatusBadge status={r.status}/></td><td className="px-3 py-3">{r.error_message&&<button aria-label="نمایش جزئیات خطا" aria-expanded={expanded===r.id} className="icon-button" onClick={()=>setExpanded(expanded===r.id?null:r.id)}><ChevronDown size={17}/></button>}</td></tr>{expanded===r.id&&<tr className="border-t" style={{borderColor:'rgb(var(--border))'}}><td colSpan={8} className="bg-red-500/[.04] px-5 py-4"><p className="text-sm font-semibold text-red-700 dark:text-red-300">علت توقف بررسی</p><p className="muted mt-2 text-xs leading-6">{r.error_message}</p></td></tr>}</Fragment>)}</tbody></table></div></div>}</div>}
