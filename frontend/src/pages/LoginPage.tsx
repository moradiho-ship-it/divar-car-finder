import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {CarFront} from 'lucide-react'; import {api} from '../api/client';
export default function LoginPage(){const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');try{const {data}=await api.post('/auth/login/',{email,password});localStorage.setItem('access',data.access);localStorage.setItem('refresh',data.refresh);nav('/')}catch{setError('ایمیل یا رمز عبور درست نیست.')}};return <div className="grid min-h-screen place-items-center bg-[#221f1d] p-5"><div className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl md:p-10"><div className="mb-8 flex items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-wine text-white"><CarFront/></div><div><h1 className="text-2xl font-extrabold">ورود به خودروبان</h1><p className="text-sm text-black/45">جستجوی خودرو، بدون جا ماندن از آگهی خوب</p></div></div><form onSubmit={submit} className="space-y-5"><label className="block"><span className="label">ایمیل</span><input dir="ltr" type="email" className="field" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="block"><span className="label">رمز عبور</span><input dir="ltr" type="password" className="field" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button className="btn-primary w-full">ورود به داشبورد</button></form></div></div>}

